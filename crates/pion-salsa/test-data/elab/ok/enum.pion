enum YesOrNo {
    Yes(_: Bool),
    No(_: Type),
}

enum Either(A: Type, B: Type) {
    Left(x: A),
    Right(y: B),
}

enum Result(T: Type, E: Type) {
    Ok(t: T): Result(T, E),
    Err(e: E): Result(T, E),
}