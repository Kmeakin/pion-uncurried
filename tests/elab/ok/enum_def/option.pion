enum Option(T: Type) {
    None,
    Some(x: T),
}

let n = None;
let s = Some;

let unwrap_or: fn(_: Option(Bool), _: Bool) -> Bool 
= fn(o: Option(Bool), d: Bool) => 
    match o {
        Some(y) => y, 
        None => d, 
    };
